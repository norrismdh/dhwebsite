# Live. /api and /dhadmin are app/admin endpoints — blocked from crawling.
# /nda, /nda-signed, /thank-you and /vs/* carry meta noindex and are left
# crawlable on purpose, so Google can read the noindex and keep them out of
# the index. (A robots Disallow would hide the noindex and risk them showing
# as URL-only results when linked externally.)
User-agent: *
Disallow: /api/
Disallow: /dhadmin/

Sitemap: https://www.digitalhive.com/sitemap.xml
